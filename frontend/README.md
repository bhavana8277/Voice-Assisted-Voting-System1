# Frontend

React and Vite client for the Voice Assisted Voting System.

## Run locally

```powershell
npm install
npm run dev
```

The development server runs on `http://localhost:5173`.

## Commands

```powershell
npm run build
npm run lint
```

The frontend expects the backend API at `http://localhost:8080/api`.
